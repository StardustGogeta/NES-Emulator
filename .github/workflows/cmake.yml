name: CMake

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  BUILD_TYPE: Release

jobs:
  build-linux-gcc:
    runs-on: ubuntu-24.04

    name: "Linux - GCC / Make"

    steps:
    - uses: actions/checkout@v4

    - name: Download SDL2
      run: sudo apt-get install libsdl2-dev

    - name: Configure CMake
      run: cmake --preset=linux-gcc-make-release-ci

    - name: Build
      run: cmake --build build --parallel

    - name: Test
      working-directory: build
      run: ctest --output-on-failure

  build-windows:
    runs-on: windows-latest

    name: Windows - ${{ matrix.make-program.name }}

    defaults:
      run:
        shell: msys2 {0}

    strategy:
      fail-fast: false
      matrix:
        make-program:
          - name: MinGW / Make
            preset: windows-mingw-make-release-ci
          - name: MinGW / Ninja
            preset: windows-mingw-ninja-release-ci
          - name: MSVC / MSBuild
            preset: windows-msvc-release-ci

    steps:
    - uses: actions/checkout@v4
    - uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: mingw-w64-x86_64-gcc mingw-w64-x86_64-make mingw-w64-x86_64-ninja
        path-type: inherit

    - name: Configure CMake
      run: cmake --preset="${{ matrix.make-program.preset }}"

    - name: Build
      # Build type required for multi-configuration generators
      run: cmake --build build --config ${{ env.BUILD_TYPE }} --parallel

    - name: Test
      working-directory: build
      # Build type required for multi-configuration generators
      run: ctest -C ${{ env.BUILD_TYPE }} --output-on-failure
